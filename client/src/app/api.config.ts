// In development this points to localhost.
// For production, replace with your Railway backend URL before running `npm run build`.
// e.g. export const API_BASE = 'https://your-app.up.railway.app/api';
export const API_BASE = 'http://localhost:5000/api';
